version: '3.9'

services:
  postgres:
    image: postgres:14-alpine
    ports:
      - 5432:5432
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=3c0mm3rc3-pr0duct
      - POSTGRES_USER=product_user
      - POSTGRES_DB=ecommerce_db
